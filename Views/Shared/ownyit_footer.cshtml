
<footer style=" position:fixed;bottom:24px;margin-left:-32px;">
    @*<div class="notification-icon">*@
        @*<ul class="dots" style="margin-bottom:0px;">
            <li>
                <a href="#" data-toggle="modal" data-target="#alert_popup" onclick="alert_message();">
                    <span class="glyphicon glyphicon-bell">
                        <mark class="big swing blue alert_count" id="alert_count"></mark>
                    </span> 
                </a> 
            </li>
        </ul>*@
      
    @*</div>*@
</footer>
<footer class="foo">
    <p style="text-align:center;color:#3075a0;margin-top: 3px;font-weight:600">Copyright &copy; @DateTime.Now.Year - Powered By TectonaS</p>
</footer>

<script type="text/javascript">
    //$(".notification12").click(function () {
    //    $("#alert_popup").modal({ backdrop: false });
    //});
</script>
<script>
    @*function alert_message() {
        $.ajax({
            type: "POST",
            url: '@Url.Content("~/csat_main_dashboard/Get_alert")',
            success: function (data) {
                //debugger;
                var SetData = $('#alert_messages');
                jQuery('#alert_messages').html('');
                var alertheader = "<table class='table table-striped table-bordered alert_datatable' style='width:100%'><thead><tr>";
                alertheader += " <th class='col-sm-1'>TimeStamp</th>" +
                    "<th class='col-sm-1 form_label'>System Name </th><th class='col-sm-1'>IP Address</th>" +
                    "<th class='col-sm-1'>User Name</th><th class='col-sm-1'>Type</th>" +
                    "<th class='col-sm-1'>Sub Type</th><th class='col-sm-1'>Status</th>" +
                    "<th class='col-sm-4'>Message</th></tr></thead><tbody>";

                var alertrows = "";
                for (var i = 0; i < data.getalert.length; i++) {
                    alertrows += "<tr>";
                    alertrows += "<td class='col-sm-1 form_label'>" + data.getalert[i].device_timestamp + "</td>" +
                        "<td class='col-sm-1 form_label'>" + data.getalert[i].device_name + "</td>" +
                        "<td class='col-sm-1'>" + data.getalert[i].ip + "</td>" +
                        "<td class='col-sm-1 form_label'>" + data.getalert[i].login_user + "</td>" +
                        "<td class='col-sm-1'>" + data.getalert[i].category_type + "</td>" +
                        "<td class='col-sm-1'>" + data.getalert[i].category_subtype + "</td>" +
                        "<td class='col-sm-1'>" + data.getalert[i].category_status + "</td>" +
                        "<td class='col-sm-4'>" + data.getalert[i].message + "</td>";
                }
                alert = alertheader + alertrows + "</tr></tbody></table>";
                SetData.append(alert);
                $(document).ready(function () {
                    $('.alert_datatable').DataTable({
                        paging: true,
                        order: [],
                        columnDefs: [{ orderable: false, targets: [] }],
                        searching: false,
                        info: false,
                        responsive: false,
                    });
                });
            }
        });
    }*@

    @*function alert_count() {
        $.ajax({
            type: "POST",
            url: '@Url.Content("~/csat_main_dashboard/Get_count")',
            success: function (data) {
                var SetData = $('#alert_count');
                jQuery('#alert_count').html('');
                var alertcount = "";
                alertcount = data.alertcount;
                //debugger;
                if (alertcount == 0) {
                    $(".alert_count").removeClass("red").addClass("blue");
                }
                else {
                    $(".alert_count").removeClass("blue").addClass("red");
                }
                SetData.append(alertcount);
            }
        });
    }*@

    @*function clear_alert() {
        //alert_count();
        $.ajax({
            type: "POST",
            url: '@Url.Content("~/csat_main_dashboard/Delete_alert")',
            success: function (data) {
                alert_count();
            }
        });
    }*@
    //$(function () {
    //    $(".newbtn").click(function () {
    //        $(".alert_count").removeClass("red").addClass("blue");
    //    });
    //});
</script>
